{
  "name": "link-beoordeelaar-backend",
  "version": "1.0.0",
  "description": "Backend of the link-beoordeelaar, that saves judgments in a Azure Cosmos DB.",
  "main": "dist/src/functions/*.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/cosmos": "^4.9.3",
    "@azure/functions": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.13.0",
    "rimraf": "^6.0.0",
    "typescript": "^6.0.0"
  }
}
